[project]
name = "aracore"
version = "0.1.0"
description = "AraCore RDT atom-to-atom mapping pipeline"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]

